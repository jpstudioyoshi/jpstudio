# Japanese Studio — Project Audit
Generated: 23/05/2026, 15:18:18
Total JS files: 43 | HTML files: 4

## Load order (from index.html)
  https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js?v=20260523c
  https://cdn.jsdelivr.net/npm/hanzi-writer@3.5/dist/hanzi-writer.min.js?v=20260523c
  https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js?v=20260523c
  src/core-foundation.js?v=20260523c
  src/core-anki.js?v=20260523c
  src/core-kana-drill.js?v=20260523c
  src/core-kana.js?v=20260523c
  src/core-counters.js?v=20260523c
  src/core-srs.js?v=20260523c
  src/core-stt.js?v=20260523c
  src/core-writing.js?v=20260523c
  src/core-listen.js?v=20260523c
  src/core-vocab.js?v=20260523c
  src/core.js?v=20260523c
  src/features-core.js?v=20260523c
  src/features-kana.js?v=20260523c
  src/features-times.js?v=20260523c
  src/briefing-prompt.js?v=20260523c
  src/GrammarModel.js?v=20260523c
  src/AppEvents.js?v=20260523c
  src/features-yoshi.js?v=20260523c
  src/features-grammar.js?v=20260523c
  src/features-reading.js?v=20260523c
  src/features-video.js?v=20260523c
  src/features-pictures.js?v=20260523c
  src/features-progress.js?v=20260523c
  src/features-stroke.js?v=20260523c
  src/features-tools.js?v=20260523c
  src/features-voice.js?v=20260523c
  src/features-voice-drill.js?v=20260523c
  src/features-custom-drill.js?v=20260523c
  src/StudentModel.js?v=20260523c
  src/IssueTracker.js?v=20260523c
  src/services/LessonSession.js?v=20260523c
  src/services/AudioService.js?v=20260523c
  src/services/PreflightService.js?v=20260523c
  src/services/TranscriptionService.js?v=20260523c
  src/services/AnalysisService.js?v=20260523c
  src/services/StorageService.js?v=20260523c
  src/Orchestrator.js?v=20260523c
  src/ui/DrillCard.js?v=20260523c
  src/ui/DrillCard.js?v=20260523c
  src/ui/TextEntry.js?v=20260523c
  src/ui/YoshiUI.js?v=20260523c

## File analysis
### check-syntax.js (660 lines)
  Functions:
    syntaxCheck() — ── Syntax check ──────────────────────────────────────────────────────────────
    extractFunctions() — 2. First comment line (// ...) immediately before the function
    buildLineIndex() — const idx = [0];
    posToLine() — let lo = 0, hi = idx.length - 1;
    extractDescription() — const idx = lineNo - 1; // line containing the function keyword
    extractFirstStatement() — const braceIdx = src.indexOf('{', fnPos);
    extractTopLevelVars() — const vars = [];
    extractWindowExports() — const exports = [];
    extractAppRegistry() — const entries = [];
    extractClaudePrompts() — Looks for: const prompt = `...`, prompt = '...', system: '...', content: '...'
    extractStringSnippet() — const quote = src[startPos];
    extractStorageWrites() — const writes = [];
    extractIpcHandles() — const handles = [];
    extractIpcInvokes() — const invokes = [];
    countIifes() — return (src.match(/\(\s*(?:async\s+)?\s*(?:function\s*\w*\s*)?\([^)]*\)\s*(?:=>\s*)?\{/g) || []).len
    analyseHtml() — ── HTML analysis ─────────────────────────────────────────────────────────────
    flagRedundant() — Flag window[] exports that are also in App registry — these are redundant
    formatFunctions() — ── Report generation ─────────────────────────────────────────────────────────
    formatPrompts() — if (!prompts.length) return '';
    formatStorageWrites() — if (!writes.length) return '';
  Top-level vars: fs, path, ROOT, TARGETS, HTML_TARGETS, SKIP, date, outFile, out, errors, ok, indexHtmlPath, totalWindowExports, totalRedundant, totalAppRegistry, totalFuncs, report, index, indexFile, ver, idxHtmlPath
  IIFEs: 2

### main.js (1227 lines)
  Functions:
    openMgmtWindow() — if (mgmtWindow && !mgmtWindow.isDestroyed()) {
    getDbPath() — return path.join(app.getPath('userData'), 'jpstudio.db');
    initDatabase()
    saveDatabase() — if (!db) return;
    createSchema() — db.run(`
    seedStaticData() — const result = db.exec('SELECT COUNT(*) as n FROM words');
    getLessonsDir() — const dir = path.join(app.getPath('userData'), 'lessons');
    findFfmpeg() — try { return execSync('which ffmpeg', { encoding: 'utf8' }).trim(); } catch(e) {}
    findPython() — const candidates = ['python3.12', 'python3', 'python'];
    createMainWindow() — mainWindow = new BrowserWindow({
    openLessonOverlay() — if (overlayWindow && !overlayWindow.isDestroyed()) {
    openYouTubeWindow() — if (youtubeWindow && !youtubeWindow.isDestroyed()) {
  Top-level vars: path, fs, mainWindow, mgmtWindow, PROJECT_DIR, youtubeWindow, overlayWindow, db, SQL
  Storage writes:
    L308 db.run('INSERT')
    L321 db.run('UPDATE')
    L672 db.run('DELETE')
  IPC handles: mgmt:openWindow, mgmt:reloadMain, print:toPDF, files:list, files:read, files:write, db:query, db:get, db:run, db:isAvailable, words:getAll, words:upsert, words:insertList, srs:getAll, srs:set, srs:setAll, srs:reset, kv:get, kv:set, kv:delete, kv:list, audio:waveform, lesson:startRecording, lesson:saveAudioChunk, lesson:finaliseRecording, lesson:chunkAudio, lesson:diarize, lesson:saveTranscript, lesson:saveNotes, lesson:get, lesson:list, lesson:deleteFile, lesson:deleteSession, lesson:getFileSizes, overlay:open, overlay:toggleOnTop, overlay:addMarker, overlay:getApiKey, overlay:startRecording, overlay:stopRecording, overlay:openTeams
  IIFEs: 9

### preload.js (77 lines)
  IPC invokes: overlay:open, audio:waveform, db:query, db:get, db:run, db:isAvailable, lesson:startRecording, lesson:saveAudioChunk, lesson:finaliseRecording, lesson:chunkAudio, lesson:diarize, lesson:saveTranscript, lesson:saveNotes, lesson:get, lesson:list, lesson:deleteFile, lesson:deleteSession, lesson:getFileSizes, kv:get, kv:set, kv:delete, kv:list, mgmt:openWindow, mgmt:reloadMain, mgmt:runCheckSyntax, mgmt:checkSyntax, files:list, files:read, files:write, print:toPDF

### preload-overlay.js (13 lines)
  IPC invokes: overlay:toggleOnTop, overlay:addMarker, overlay:getApiKey, overlay:startRecording, overlay:stopRecording, overlay:openTeams

### src/core-foundation.js (1917 lines)
  Functions:
    escHtml() — ╚══════════════════════════════════════════════════════════════════════════════
    formatDate() — if (!ts) return '';
    translateCacheSave() — const keys = Object.keys(_translateCache);
    _corpusWriteLookup() — Write a single translate-cache entry into corpus_entries + corpus_lookups.
    qtHistoryAdd() — if (!word || /^(KANJI|READING|MEANING|DICT|JAPANESE):/i.test(word)) return;
    qtHistoryUpdate() — const menu = document.getElementById('globalQTHistoryMenu');
    toggleQTHistory() — e.stopPropagation();
    selectQTHistory() — const input = document.getElementById('globalQTInput');
    globalQTSelectHistory() — if (!word) return;
    globalQuickTranslate() — const input = document.getElementById('globalQTInput');
    globalQTSpeak() — if (!_qtLastJapanese) return;
    globalQTClear() — _qtLastJapanese = '';
    renderLookupsTable() — const tbody    = document.getElementById('lookupsTableBody');
    lookupDelete() — const tmp = document.createElement('div');
    clearLookupCache() — if (!confirm('Clear all cached translations?')) return;
    apiUsageLoad() — AppState.apiUsage — see declaration below
    apiUsageSave() — Storage.set(STORAGE_KEYS.API_USAGE, JSON.stringify(AppState.apiUsage));
    apiUsageTrack() — AppState.apiUsage.inputTokens += inputTokens;
    apiUsageGetCost() — return (AppState.apiUsage.inputTokens * COST_PER_INPUT_TOKEN) + (AppState.apiUsage.outputTokens * CO
    apiUsageReset() — if (confirm('Reset API usage tracking? This cannot be undone.')) {
    recordError() — Writes one row to error_history with a full timestamp.
    getApiKey() — return Storage.getApiKey(); }
    getOpenAIKey() — return Storage.getOpenAIKey(); }
    claudeAPI() — const apiKey = getApiKey();
    claudeText() — return data.content?.[0]?.text || '';
    saveApiKey() — const val = document.getElementById('apikeyInput').value.trim();
    saveOpenAIKey() — saveHFToken removed — diarization removed
    exportAllData() — localStorage interceptor removed — localStorage.setItem is not patchable in Electron renderer
    importDataPrompt() — const input = document.createElement('input');
    clearAllData() — Storage.clearAll();
    loadState() — STATE
    saveState() — Storage.setState(state); }
    updateStreak() — const today = new Date().toDateString();
    initApiKeyBar() — API KEY BAR
    wordsSwitchSub() — ═══════════════════════════════════════════════════════
    showPanel() — const currentPanel = document.querySelector('.panel.active');
    stSwitchTab() — const panes = { settings: 'stPaneSettings', context: 'stPaneContext', grammar: 'stPaneGrammar' };
    stRenderContext() — const el = document.getElementById('stContextDisplay');
    stCopyContext()
    openDevWindow() — if (window.mgmt?.openWindow) {
    … +3 more
  Top-level vars: App, COUNTER_DATA, RTK_KANJI, KANJI_JLPT_LEVEL, KANJI_MEANINGS, JLPT_WORDS, HIRAGANA, KATAKANA, KATAKANA_DAKUTEN, KATAKANA_HANDAKUTEN, KATAKANA_EXTENDED, GRAMMAR_POINTS, DEFAULT_VOCAB, VERBS_U, VERBS_RU, VERBS_IRR, ADJ_I, ADJ_NA, _dataLoaded, TRANSLATE_CACHE_KEY, QT_HISTORY_KEY, _translateCache, _qtHistory, _qtLastJapanese, transHistory, transLastParsed, chatHistory, lastUserQuestion, SYSTEM_PROMPT, writingSentences, writingChatHistory, AppState, WRITING_SYSTEM, COST_PER_INPUT_TOKEN, COST_PER_OUTPUT_TOKEN, STORAGE_KEYS, STORAGE_BACKUP_KEYS, LOCAL_ONLY_KEYS, PURGE_KEYS, MIGRATE_PREFIXES, _kvCache, _kvReady, Storage, state, _kanaLastMode
  window[] exports: x, globalQuickTranslate, globalQTClear, toggleQTHistory, renderLookupsTable, clearLookupCache, stSwitchTab, openDevWindow
  App registry: translate, globalQuickTranslate, globalQTClear, globalQTSelectHistory, qtHistoryUpdate, toggleQTHistory, selectQTHistory, renderLookupsTable, clearLookupCache, management, exportAllData, importDataPrompt, clearAllData, Settings, stSwitchTab, openDevWindow, stRenderGrammarOverride, stGrammarSetOverride, stRenderContext, stCopyContext, setButtonGroupActive, Navigation, showPanel, utilities, escHtml, formatDate, claudeAPI, claudeText, getApiKey, getOpenAIKey, saveApiKey, saveOpenAIKey, recordError, Storage, STORAGE_KEYS, state, saveState, usage, apiUsageLoad, apiUsageSave, apiUsageTrack, apiUsageGetCost, apiUsageReset, Init, initApiKeyBar
  ⚠ Redundant window[] exports (also in App registry): globalQuickTranslate, globalQTClear, toggleQTHistory, renderLookupsTable, clearLookupCache, stSwitchTab, openDevWindow
  Claude prompts:
    L580 prompt: "Translate this Japanese word/phrase to English. Reply in this exact format only:\nKANJI: [kanji/dictionary form — leave empty if input is already plain kana]\nR…"
    L582 prompt: "Translate this English word/phrase to Japanese. Reply in this exact format only:\nKANJI: [kanji form if applicable, otherwise leave empty]\nREADING: [hiragana o…"
  Storage writes:
    L499 Storage.set('TRANSLATE_CACHE')
    L894 Storage.set('API_USAGE')
    L1585 Storage.set('GRAM_NOTES')
    L136 db.run('INSERT')
  IIFEs: 30

### src/core-anki.js (451 lines)
  Functions:
    ankiInvoke()
    ankiConnect() — const dot = document.getElementById('ankiStatusDot');
    ankiLoadDecks() — if (!AnkiState.connected) return;
    ankiTodayKey() — const d = new Date();
    ankiSaveSession() — const key = ankiTodayKey(AnkiState.currentDeck);
    ankiLoadSession()
    ankiLoadDeck() — if (!AnkiState.connected) return;
    ankiShowNextCard() — if (AnkiState.dueCards.length === 0) {
    ankiRenderCard() — const frontEl = document.getElementById('ankiCardFront');
    ankiPlayAudio() — const filename = AnkiState.audioFiles[idx];
    ankiToggleDirection() — AnkiState.direction = AnkiState.direction === 'jp-en' ? 'en-jp' : 'jp-en';
    ankiSpeakFront() — const jpText = AnkiState.direction === 'jp-en' ? AnkiState.cardFrontText : AnkiState.cardBackText;
    ankiShowAnswer() — document.getElementById('ankiCardBack').style.display = 'block';
    ankiAnswer() — if (!AnkiState.currentCard) return;
  Top-level vars: AnkiState
  window[] exports: ankiConnect, ankiLoadDeck, ankiShowAnswer, ankiAnswer, ankiToggleDirection, ankiConnect, ankiPlayAudio, ankiLoadDeck, ankiShowAnswer, ankiAnswer, ankiToggleDirection, ankiSpeakFront
  App registry: AnkiState, ankiTodayKey, ankiSaveSession, ankiLoadSession, ankiRenderCard, ankiToggleDirection, ankiSpeakFront, ankiShowAnswer
  ⚠ Redundant window[] exports (also in App registry): ankiShowAnswer, ankiToggleDirection, ankiShowAnswer, ankiToggleDirection, ankiSpeakFront

### src/core-kana-drill.js (634 lines)
  Functions:
    setWordsChallengeMode() — KanaDrillState.wordsChallengeMode = mode;
    setWordsCategory() — KanaDrillState.wordsCategory = cat;
    checkMultiChoice() — if (kanaCardTimerRevealed) return;
    setKanaTimer() — kanaTimerSeconds = parseInt(s); }
    kanaToggleHint() — const show = true; // hint always shown
    setKanaMode() — const _timerRow    = document.getElementById('kanaTimerRow');
    toggleKanaDakuten() — KanaDrillState.dakutenEnabled = document.getElementById('kanaDakutenCheck')?.checked || false;
  Top-level vars: KANA_WORDS, KM, KanaDrillState, kanaTimerSeconds, kanaCardTimer, kanaCardTimerRevealed, KANA_SESSION_RUNS, WORDS_CATEGORIES
  window[] exports: toggleKanaDakuten, KM, KanaDrillState, toggleKanaDakuten
  App registry: KANA_WORDS, KM, KanaDrillState, setWordsChallengeMode, setWordsCategory, checkMultiChoice, setKanaTimer, kanaToggleHint, setKanaMode, toggleKanaDakuten
  ⚠ Redundant window[] exports (also in App registry): toggleKanaDakuten, KM, KanaDrillState, toggleKanaDakuten
  IIFEs: 1

### src/core-kana.js (748 lines)
  Functions:
    wSrsSave() — WORD_SRS_KEY kept for Storage reference only — data now managed by DrillSRS
    wIsMastered() — const d = DrillSRS.get(STORAGE_KEYS.DRILL_SRS_WORDS, kana);
    wMasteryStats() — const all = KANA_WORDS.katakana;
    wSrsRecord() — DrillSRS.record(STORAGE_KEYS.DRILL_SRS_WORDS, kana, correct);
    wSrsBuildPool() — - Recently mastered words fill remaining slots at reduced rate
    initKanaDrill() — stopCardTimer();
    kanaStartFreshSession() — stopCardTimer();
    kanaResumeSession() — stopCardTimer();
    kanaUpdateSessionUI() — const el = document.getElementById('kanaSessionInfo');
    kanaNextRun() — KanaDrillState.sessionRun++;
    kanaShowSessionSummary() — stopCardTimer();
    kanaShowMastery() — stopCardTimer();
    nextKana() — stopCardTimer();
    kanaSpeakCurrent() — if (KanaDrillState.currentKana && KanaDrillState.currentKana.char) {
    setupMultipleChoice() — const btns = document.querySelectorAll('.multi-choice-btn');
    startCardTimer() — let timeLeft = kanaTimerSeconds;
    stopCardTimer() — if (kanaCardTimer) { clearInterval(kanaCardTimer); kanaCardTimer = null; }
    cardTimerExpired() — if (!KanaDrillState.currentKana || kanaCardTimerRevealed) return;
    checkAnswer() — const _inp = document.getElementById('answerInput');
    skipKana() — if (!KanaDrillState.currentKana) {
    handleKanaKey() — if (e.key !== 'Enter') return;
    handleKanaInput() — if (kanaCardTimerRevealed) return; // wrong answer pending Enter
    updateDrillStats() — document.getElementById('drillCorrect').textContent = KanaDrillState.correctCount;
    renderDrillProgress() — const el = document.getElementById('drillProgress');
    showDrillComplete() — stopCardTimer();
    renderKanaGrid() — const allKana = (KanaDrillState.mode === 'katakana' || KanaDrillState.mode === 'words') ? KATAKANA :
  Top-level vars: WORD_SRS_KEY, WS
  window[] exports: kanaSpeakCurrent, WS, kanaShowMastery, kanaStartFreshSession, kanaSpeakCurrent, initKanaDrill
  App registry: WS, wSrsSave, wIsMastered, wMasteryStats, wSrsRecord, wSrsBuildPool, initKanaDrill, kanaStartFreshSession, kanaResumeSession, kanaUpdateSessionUI, kanaNextRun, kanaShowSessionSummary, kanaShowMastery, nextKana, kanaSpeakCurrent, setupMultipleChoice, startCardTimer, stopCardTimer, cardTimerExpired, checkAnswer, skipKana, handleKanaKey, handleKanaInput, updateDrillStats, renderDrillProgress
  ⚠ Redundant window[] exports (also in App registry): kanaSpeakCurrent, WS, kanaShowMastery, kanaStartFreshSession, kanaSpeakCurrent, initKanaDrill
  IIFEs: 7

### src/core-counters.js (1393 lines)
  Functions:
    _counterNounEmoji() — if (typeof VG_CATEGORIES === 'undefined') return '';
    _loadCounterNouns() — if (_counterNounCache) return _counterNounCache;
    cmLevenshtein() — Infer whether a wrong answer was wrong counter group or wrong phonetics
    cmInferErrorType() — if (!input || !COUNTER_DATA) return 'phonetic';
    countInit2() — countRenderRefGrid2();
    countRenderRefGrid2() — const grid = document.getElementById('countRefGrid2');
    countShowRefPopup2() — const counter = COUNTER_DATA[counterKey];
    countUpdateOptions2() — CounterDrillState.activeCounters = [];
    countBuildPool2() — const pool = [];
    countStartSession2() — if (CounterDrillState.activeCounters.length === 0) {
    countForceNewSession2() — CounterDrillState.run = 1;
    countResumeSession2() — CounterDrillState.queue = saved.queue.map(item => {
    countBuildRunQueue2() — const pool = countBuildPool2();
    _countShowStandard() — const idx = Math.floor(Math.random() * item.counter.emoji.length);
    countToggleObjectMode() — const btn = document.getElementById('countObjectModeBtn');
    countShowQuestion2() — if (CounterDrillState.idx >= CounterDrillState.queue.length) {
    countCheck2() — if (CounterDrillState.run === 0) { countStartSession2(); return; }
    countAdvance2() — if (DrillFlow._advancing) return;
    countEndRun2() — const pct = Math.round((CounterDrillState.correct / CounterDrillState.queue.length) * 100);
    countShowSummary2() — const problems = Object.entries(CounterDrillState.sessionWrong)
    countRenderProgress2() — const el = document.getElementById('countProgress2');
    countUpdateUI2() — const runEl = document.getElementById('countRunIndicator2');
    countHear2() — if (CounterDrillState.current) {
    countShowLookup2() — const _escLookup = (e) => {
    countShowMastery() — const status = CM.getAllStatus();
    daysToggleTable() — const tableEl = document.getElementById('daysOfMonthTable');
    daysStop() — const drillEl = document.getElementById('daysDrillArea');
    daysOfMonthInit() — const tableEl = document.getElementById('daysOfMonthTable');
    daysOfMonthDrill() — const drillEl = document.getElementById('daysDrillArea');
    daysNext() — function daysCheck() {}
    daysCheck() — let _daysChecked = false;
    conjugate() — const t = word.type;
    goalsLoad()
    goalsSave() — const g = {
    goalsRestoreUI() — const g = goalsLoad();
    getConjOptions() — const verbTypes = [];
    startConjDrill() — const {verbTypes, forms, polarities, registers} = getConjOptions();
    renderConjDrill() — const area = document.getElementById('conjDrillArea');
    handleConjKey() — if (e.key === 'Enter') {
    conjAutoConvert() — if (!conjCurrentAnswer) return;
    … +1 more
  Top-level vars: COUNT_SESSION_RUNS, COUNT_QUESTIONS_PER_RUN, _counterNounCache, CounterDrillState, CM, _daysQueue, DAYS_OF_MONTH, _daysChecked, teMap, masuMap, naiMap, taMap, volMap, GOALS_DEFAULTS, CONJ_QUESTIONS_PER_RUN, CONJ_SESSION_RUNS, conjQueue, conjResults, conjTypedAnswers
  window[] exports: countCheck2, countShowMastery, countAdvance2, countHear2, countShowLookup2, daysToggleTable, daysStop, daysOfMonthDrill, daysNext, daysCheck
  App registry: CM, countShowRefPopup2, countInit2, countStartSession2, countUpdateOptions2, daysOfMonthInit
  Storage writes:
    L117 Storage.set('COUNTER_MASTERY')
    L214 Storage.set('COUNTER_SESSION')
    L1222 Storage.set('GOALS')
  IIFEs: 5

### src/core-srs.js (578 lines)
  Functions:
    rateListen() — ╚══════════════════════════════════════════════════════════════════════════════
    toggleSrsTracking() — ╚══════════════════════════════════════════════════════════════════════════════
    ctrRefInit() — ctrRefRenderGrid();
    ctrRefRenderGrid() — const el = document.getElementById('ctrRefGrid');
    ctrRefSetCompare() — Track which counter was last clicked for quick compare
    ctrRefPopulateSelects() — const selA = document.getElementById('ctrCompareA');
    ctrRenderCompare() — const keyA = document.getElementById('ctrCompareA')?.value;
    kanjiCorpusGet() — ═══════════════════════════════════════════════════════
    kanjiCorpusSave() — (App.Storage || window.Storage).setJSON(STORAGE_KEYS.KANJI_CORPUS, data);
    _corpusEntriesUpsert() — if (typeof window === 'undefined' || !window.db) return;
    kanjiExtract() — return [...(str || '')].filter(ch => {
    kanjiCorpusRecordLookup() — const kanji = kanjiExtract(word);
    kanjiCorpusRecordChatProduction() — const kanji = kanjiExtract(text);
    kanjiCorpusRecordProduction() — const kanji = kanjiExtract(text);
  Top-level vars: DrillSRS, CTR_PROBLEM_NUMS
  App registry: DrillSRS, rateListen, toggleSrsTracking, ctrRefInit, ctrRefRenderGrid, ctrRefSetCompare, ctrRefPopulateSelects, ctrRenderCompare, kanjiCorpusGet, kanjiCorpusSave, kanjiExtract, kanjiCorpusRecordLookup, kanjiCorpusRecordChatProduction, kanjiCorpusRecordProduction
  Storage writes:
    L98 db.run('INSERT')
    L230 db.run('DELETE')
  IIFEs: 7

### src/core-stt.js (842 lines)
  Functions:
    sttStart() — const btn = document.getElementById(buttonId);
    sttStop() — if (SttState.mediaRecorder && SttState.recording) {
    sttTranscribe() — const openaiKey = getOpenAIKey();
    refreshMicrophones() — ═══════════════════════════════════════════════════════
    setMicrophone() — SttState.selectedMicId = deviceId;
    testMicrophone() — const btn = document.getElementById('micTestBtn');
    stopMicTest() — if (SttState.testStream) {
    startWritingSpeech() — sttStart('writingInput', 'writingSpeechBtn', 'ja');
    detectJapanese() — ═══════════════════════════════════════════════════════
    handleTransKey() — if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); doTranslate(); }
    clearTranslate() — const sp = document.getElementById('transSpellingInline');
    setTransMode() — AppState.transKanaOn — see declaration below
    toggleTransKana() — const inp = document.getElementById('transInput');
    _transKanjiKeyHandler() — if (e.key === '+') {
    doTranslate() — const text = document.getElementById('transInput').value.trim();
    escBdHtml() — return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\n/g,'<br>
    showBdTab() — document.querySelectorAll('.trans-breakdown-tab').forEach(b => b.classList.remove('active'));
    transShowHistory() — const sessionTab = document.getElementById('transSessionTab');
    renderTransHistory() — const el = document.getElementById('transSessionList');
    loadTransHistory() — const h = transHistory[i];
    transToggleStar() — if (transHistory[i]) {
    transSpeakHistoryItem() — const h = transHistory[i];
    transDeleteHistoryItem() — if (i >= 0 && i < transHistory.length) {
    transEditHistoryItem() — const h = transHistory[i];
    transClearHistory() — if (transHistory.length === 0) return;
    transExportHistory() — if (transHistory.length === 0) {
    startTransSpeech() — const currentText = document.getElementById('transInput')?.value || '';
    _conjRecordGrammarEvidence()
    toHiragana() — return str.replace(/[ァ-ヶ]/g, ch => String.fromCharCode(ch.charCodeAt(0) - 0x60));
    checkConj() — const input = document.getElementById('conjInput');
    advanceConj() — if (DrillFlow._advancing) return;
  Top-level vars: SttState, transSpeechRec, CONJ_TO_GRAMMAR_NODE, listenTracks, listenCurrentIdx, listenAbA, listenLoopPauseSecs
  window[] exports: transShowHistory
  Storage writes:
    L210 Storage.set('SELECTED_MIC')
  IIFEs: 9

### src/core-writing.js (686 lines)
  Functions:
    writingErrorsLoad() — ═══════════════════════════════════════════════════════
    writingErrorsSave() — (App.Storage || window.Storage).set(STORAGE_KEYS.WRITING_ERRORS_ALT, JSON.stringify(AppState.writing
    normaliseParticlePattern() — const p = pattern.toLowerCase();
    writingErrorsAdd() — if (!errorType || errorType === 'none' || !pattern) return;
    _writingErrorRecordGrammarEvidence()
    writingErrorsGetTopPatterns() — return [...AppState.writingErrors].sort((a, b) => b.count - a.count).slice(0, n);
    writingErrorsClear() — if (!confirm('Clear all written error history? This cannot be undone.')) return;
    errorDenylistLoad() — try { return JSON.parse((App.Storage || window.Storage).get(ERROR_DENYLIST_KEY) || '[]'); } catch(e)
    errorDenylistSave() — (App.Storage || window.Storage).set(ERROR_DENYLIST_KEY, JSON.stringify(list));
    errorDenylistAdd() — const list = errorDenylistLoad();
    errorDenylistRemove() — const list = errorDenylistLoad().filter(p => p !== pattern);
    wbCallTutor() — if (!(App.getApiKey || window.getApiKey)?.()) { alert('Please add your API key using the ⚙ API butto
    checkWritingSentence() — const input = document.getElementById('writingInput');
    submitWritingSentence() — const input = document.getElementById('writingInput');
    wbAddItem() — AppState.wbDragEl — see declaration below
    wbDelete() — item.remove();
    wbRenumber() — document.querySelectorAll('#writingBoard .wb-item').forEach((el, i) => {
    wbUpdateCount() — const n = writingSentences.length;
    wbDragStart() — AppState.wbDragEl = this;
    wbDragOver() — e.preventDefault();
    wbDragLeave() — this.classList.remove('drag-over');
    wbDrop() — e.preventDefault();
    wbDragEnd() — if (AppState.wbDragEl) AppState.wbDragEl.classList.remove('dragging');
    renderTextSoFar() — wbUpdateCount(); } // legacy stub
    wbCopyInput() — const inp = document.getElementById('writingInput');
    renderFeedback() — const container = document.getElementById('feedbackEntries');
    feToggle() — const collapsed = el.classList.toggle('fe-collapsed');
    toggleDetail() — const el = document.getElementById(id);
    deleteWritingEntry() — writingSentences.splice(idx, 1);
    handleWritingKey() — if (e.key === 'Enter' && (e.ctrlKey || e.metaKey)) { e.preventDefault(); submitWritingSentence(); }
    clearWritingFeedback() — document.getElementById('feedbackEntries').innerHTML = '<div class="feedback-empty">Check or submit 
    saveWritingText() — if (writingSentences.length === 0) { alert('Nothing to save yet!'); return; }
    renderSavedTexts() — const saved = (App.Storage || window.Storage).getStudioTexts();
    copySavedText() — const saved = (App.Storage || window.Storage).getStudioTexts();
    deleteSavedText() — const saved = (App.Storage || window.Storage).getStudioTexts();
    _tokenExistsInOriginal() — const tLen = token.length;
    highlightCorrectedParticles() — if (!original || !corrected || original === corrected) return escHtml(corrected);
  Top-level vars: WRITING_ERROR_TO_NODE, ERROR_DENYLIST_KEY, _writingCheckedSentences, writingSpeechRec
  window[] exports: errorDenylistAdd, errorDenylistRemove
  App registry: writingErrorsLoad, writingErrorsSave, writingErrorsAdd, writingErrorsGetTopPatterns, writingErrorsClear, errorDenylistLoad, errorDenylistSave, errorDenylistAdd, errorDenylistRemove, normaliseParticlePattern, wbAddItem, wbDelete, wbRenumber, wbUpdateCount, highlightCorrectedParticles
  ⚠ Redundant window[] exports (also in App registry): errorDenylistAdd, errorDenylistRemove
  IIFEs: 7

### src/core-listen.js (2196 lines)
  Functions:
    readId3Title() — Instead we read the ID3v2 tag from the file bytes to get the real title.
    tryShiftJIS()
    tryDecodeFilename() — if (/[\u3040-\u30ff\u4e00-\u9fff]/.test(name)) return name; // already Japanese
    loadListenFiles() — const audioExts = /\.(mp3|m4a|wav|ogg|aac|flac|opus|weba|mp4)$/i;
    handleListenDrop() — e.preventDefault();
    readEntry() — return new Promise(resolve => {
    showListenLayout() — if (!listenTracks.length) return;
    _ensureDictPanelState() — if (document.getElementById('dictationPanel').style.display === 'none') return;
    renderListenPlaylist() — const el = document.getElementById('listenPlaylist');
    loadAndPlayTrack() — loadListenTrack(idx, false);
    removeListenTrack() — if (idx < 0 || idx >= listenTracks.length) return;
    toggleSelectAll() — document.querySelectorAll('.listen-check').forEach(cb => cb.checked = checked);
    updateSelectAll() — const all = document.querySelectorAll('.listen-check');
    getCheckedIndices() — return Array.from(document.querySelectorAll('.listen-check:checked'))
    playSelected() — const sel = listenTracks[listenCurrentIdx >= 0 ? listenCurrentIdx : 0];
    loadListenTrack() — autoplay = false; // Manual play only
    listenQueueNext() — if (listenQueue.length && listenQueuePos >= 0) {
    listenUpdateNavBtns() — const prevBtn = document.querySelector('.listen-transport-btn[title="Previous"]');
    listenQueuePrev() — if (listenQueue.length && listenQueuePos > 0) {
    toggleListenPlay() — const audio = document.getElementById('listenAudio');
    listenTransToggleKana() — Translation cache is defined earlier in the file (GLOBAL QUICK TRANSLATE section)
    listenTransClear() — const inp = document.getElementById('listenTranscribeText');
    renderListenSession() — const list = document.getElementById('listenSessionList');
    listenSessionDelete() — if (i >= 0 && i < ListenState.sessionLookups.length) {
    updatePlayBtn() — const btn = document.getElementById('listenPlayBtn');
    listenOnEnded() — listenPlaying = false; updatePlayBtn();
    listenTimeUpdate() — const audio = document.getElementById('listenAudio');
    setListenLoopPause() — listenLoopPauseSecs = secs;
    setListenSpeed() — listenCurrentSpeed = rate;
    listenSkip() — const audio = document.getElementById('listenAudio');
    fmtTime() — if (!s || isNaN(s)) return '0:00';
    cycleLoop() — if (listenAbLooping || loopSetState === 'a' || loopSetState === 'b') {
    updateLoopSetUI() — kept as alias for safety
    clearAbLoop() — listenAbA = null; listenAbB = null; listenAbLooping = false; loopSetState = null;
    waveformClick() — const canvas = document.getElementById('listenWaveform');
    updateListenTimeDisplay() — const audio = document.getElementById('listenAudio');
    saveListenNote() — clearTimeout(listenNotesSave);
    clearListenPlaylist() — listenTracks.forEach(t => URL.revokeObjectURL(t.url));
    decodeWaveform() — const waveMsg = document.getElementById('listenWaveMsg');
    findSilenceBoundary() — direction: 'before' scans leftward, 'after' scans rightward.
    … +48 more
  Top-level vars: ListenState, listenNotesSave, listenPlaying, listenCurrentSpeed, listenAudioCtx, listenWaveData, listenQueue, listenQueuePos, loopSetState, listenAudioBufRef, N5_SENTENCES, dictSentence, dictationActive, dictTimeoutSecs, dictResumeTimer, dictCountdownTimer, dictCountdownVal, dictTyping, shadowingActive, shadowSegSecs, shadowRepCount, shadowRepsDone, shadowSegStart, shadowPauseSecs, shadowPauseTimer, VOCAB_BM_KEY, vocabDefaultOffset, SRS
  window[] exports: listenClearSegments, listenPlayAllSegments, listenCombineSegments, listenDownloadSegments, setListenRecMode
  App registry: ListenState, readId3Title, tryShiftJIS, tryDecodeFilename, handleListenDrop, showListenLayout, renderListenPlaylist, loadAndPlayTrack, removeListenTrack, toggleSelectAll, updateSelectAll, toggleListenPlay, dropVocabBookmark
  Claude prompts:
    L1060 prompt: "The student is practising Japanese dictation at N5 level. The sentence that was played: 「${…}」 The student wrote: 「${…}」 Give brief, direct feedback in English:…"
  IIFEs: 24

### src/core-vocab.js (1451 lines)
  Functions:
    isWordMastered() — ═══════════════════════════════════════════════════════
    toggleVcReading() — if (e) e.stopPropagation();
    startNewSession() — if (!_dataLoaded || !state.vocab.length) return;
    getSessionDeck() — return vocabSession.filter(i => !_sessionKnown[i]);
    markVocab() — if (!vocabSession.length) return;
    renderVocab() — const vocabCardEl   = document.getElementById('vocabCard');
    prevVocab() — const deck = getSessionDeck();
    nextVocab() — const deck = getSessionDeck();
    flipVocab() — vocabFlipped = !vocabFlipped;
    toggleVcDirection() — vcDirection = vcDirection === 'jp-en' ? 'en-jp' : 'jp-en';
    resetVocabDeck() — state.vocabProgress = {};
    toggleVocabList() — const list = document.getElementById('vocabList');
    renderVocabList() — const container = document.getElementById('vocabList');
    deleteVocabCard() — state.vocab.splice(idx, 1);
    printVocabList() — const cards = state.vocab;
    vcFetch() — const category = document.getElementById('vcFetchCategory').value.trim();
    vcFetchSelectAll() — document.querySelectorAll('.vc-fetch-check').forEach(el => el.checked = checked);
    vcFetchAdd() — const words = window._vcFetchWords || [];
    vcToggleHint() — const hint = document.getElementById('vcHint');
    transAddToVocab() — const src = document.getElementById('transInput').value.trim();
    renderGrammar() — GRAMMAR
    toggleGrammar() — const el = document.getElementById('ge' + i);
    appendChatMsg() — ═══════════════════════════════════════════════════════
    updateChatMsg() — const el = document.getElementById(id);
    transSpeakOutput() — const inp = document.getElementById('transInput').value.trim();
    chatQuick() — const input = document.getElementById('chatInput');
    sendChat() — const input = document.getElementById('chatInput');
    startChatSpeech() — sttStart('chatInput', 'chatSpeechBtn', 'ja');
    kanjiCorpusFilter() — KanjiCorpusState — see above
    kanjiCorpusSort() — KanjiCorpusState.sort = sort;
    kanjiSetView() — KanjiCorpusState — see below
    kanjiCorpusRender() — const grid   = document.getElementById('kanjiCorpusGrid');
    kanjiCorpusShowDetail() — const detail = document.getElementById('kanjiCorpusDetail');
    vcFilter() — VocabCorpusState — see below
    vcSort() — VocabCorpusState.sort = s;
    vcLevels() — const levels = new Set();
    vcRenderTargets() — Target words are now surfaced via the priority score in the Words drill.
    vcRenderTargetsInline() — const list = document.getElementById('vcTargetsList2');
    vcDrillWord() — try { gramSentPracticeError(word + ' (' + meaning + ')'); } catch(e) {}
    vocabPriorityContext() — Returns a compact text block summarising vocabulary state for Claude.
    … +7 more
  Top-level vars: vocabIdx, vocabFlipped, vcReadingVisible, vcDirection, vocabSession, vocabSessionPos, _sessionKnown, chatRecognition, KanjiCorpusState, VocabCorpusState
  App registry: toggleVcDirection, vcRenderTargetsInline, vcDrillWord, vcRenderTargets, wordPriorityScore, wordEnrichWithSRS, vcBuildPriorityList, vocabPriorityContext, startNewSession, renderVocab, markVocab, isWordMastered, renderGrammar
  Claude prompts:
    L457 prompt: "Give me ${…} Japanese vocabulary words in the category: "${…}". Level: N5-N4 beginner. Include common, practical words. Reply ONLY with a JSON array, no markdow…"
  IIFEs: 8

### src/core.js (591 lines)
  Functions:
    updateProgressPanel() — ═══════════════════════════════════════════════════════
    progressRenderCost() — const container = document.getElementById('progress-cost-display');
    getGramData() — return Storage.getJSON(STORAGE_KEYS.GRAM_NOTES, {});
    saveGramData() — Storage.setJSON(STORAGE_KEYS.GRAM_NOTES, data);
    migrateTextToEntries() — return text.split(/\n---\n/).map(chunk => {
    getGramEntries() — const data = getGramData();
    loadGramNotes() — escHtml moved to core-foundation.js
    gramNotesShowTab() — GRAM_TABS.forEach(t => {
    renderGramEntries() — const container = document.getElementById('gnote-entries-' + tab);
    addGramEntry() — const input = document.getElementById('gnote-input-' + tab);
    deleteGramEntry() — const data = getGramData();
    moveGramEntry() — if (!toTab || toTab === fromTab) return;
    editGramEntry() — const entryEl = document.getElementById('gentry-' + tab + '-' + idx);
    saveGramEntryEdit() — const entryEl = document.getElementById('gentry-' + tab + '-' + idx);
    cancelGramEntryEdit() — renderGramEntries(tab); }
    gramSearch() — const resultsEl = document.getElementById('gramSearchResults');
    showGramTab() — document.querySelectorAll('.gramnote-tab').forEach(b => b.classList.remove('active'));
    showGramTabAndHighlight() — document.getElementById('gramSearchInput').value = '';
    htmlToPlainMarkdown() — function nodeToText(node, inTable) {
    openGramSendDialog() — const msgEl = document.getElementById(msgId);
    sendToGramNote()
    saveListenPrefs()
    restoreListenPrefs()
  Top-level vars: GRAM_TABS, GRAM_NOTES_KEY, GRAM_TAB_LABELS, LISTEN_PREFS_KEY
  Storage writes:
    L26 Storage.set('agentLastProgressView')
    L115 Storage.set('GRAM_NOTES')
    L437 Storage.set('LISTEN_TRACK_NAMES')
  IIFEs: 9

### src/features-core.js (755 lines)
  Functions:
    ctrDrillPool() — Selected counter indices for drill — default: all people + objects + time counters
    ctrToggleDrill() — if (cb.checked) CtrDrillState.selected.add(idx);
    toHiraganaSimple() — return s.replace(/[ァ-ヶ]/g, ch => String.fromCharCode(ch.charCodeAt(0) - 0x60));
    ctrMeaningMatch() — const norm = s => s.toLowerCase().replace(/[^a-z ]/g, '').trim();
    jpSpeak() — return TTS.speak(text, rate || 0.9, opts || {}); }
    setTtsVoice() — ── Legacy shims ─────────────────────────────────────────
    testVoice() — TTS.test(); }
    vvToggle() — TTS.vvSetEnabled(on); }
    vvSetSpeaker() — TTS.vvSetSpeaker(id); }
    toggleResourcesSettings() — const panel = document.getElementById('resourcesSettingsPanel');
    vocabSpeak() — const _state    = App.state    || window.state;
    conjSpeak() — const _conjCurrentAnswer = App.conjCurrentAnswer || window.conjCurrentAnswer;
    toggleConjListenMode() — conjListenMode = !conjListenMode;
    trimSilence() — ═══════════════════════════════════════════════════════
    audioBufferToWav() — const numChannels = buffer.numberOfChannels;
  Top-level vars: DrillFlow, COUNTERS, CtrDrillState, ctrDrillCat, CTR_SYNONYMS, TTS, vocabListenMode, _origRenderVocab, conjListenMode
  window[] exports: jpSpeak, setTtsVoice, testVoice, vvToggle, vvSetSpeaker, vocabSpeak, conjSpeak, toggleConjListenMode, toggleResourcesSettings, audioBufferToWav, trimSilence
  App registry: TTS, jpSpeak, setTtsVoice, testVoice, vvToggle, vvSetSpeaker, helpers, vocabSpeak, conjSpeak, toggleConjListenMode, panel, toggleResourcesSettings, utilities, trimSilence, audioBufferToWav, DrillFlow, ctrDrillPool, ctrToggleDrill, ctrMeaningMatch, toHiraganaSimple
  ⚠ Redundant window[] exports (also in App registry): jpSpeak, setTtsVoice, testVoice, vvToggle, vvSetSpeaker, vocabSpeak, conjSpeak, toggleConjListenMode, toggleResourcesSettings, audioBufferToWav, trimSilence
  IIFEs: 4

### src/features-kana.js (1077 lines)
  Functions:
    ROMAJI_MAP() — _kanaLastMode declared in core.js — shared across all kana inputs
    romajiToKatakana() — const hira = romajiToHiragana(str);
    vgSetupInput() — const inp = document.getElementById('vg-input');
    romajiToHiragana() — let out = '';
    kanaInputHandler() — Then call: setButtonGroupActive('groupName', 'activeButtonId')
    hiraganaToKatakana() — return str.split('').map(c => {
    kanaOn() — if (!el || el._kanaOn) return;
    kanaOff() — if (!el) return;
    kanaToggle() — if (!el._kanaOn) {
    kanaAddToggle() — if (!el) return;
    kanjiPickerShow() — kajiPickerClose();
    kajiPickerClose() — document.querySelectorAll('.kanji-picker').forEach(p => {
    kanaToKanji() — if (document.querySelector('.kanji-picker')) { kajiPickerClose(); return; }
    kanaSetMode() — const inp = document.getElementById(inputId);
    kanaToolbar() — Guards against double-init: safe to call on every panel render.
    _initKanaToolbars() — console.log('[kana] _initKanaToolbars running, readyState=', document.readyState);
    setGlobalQTMode() — ── Mode-setter shims (kept for programmatic callers) ────────────────────────
    setChatInputMode() — All now reference IDs generated by kanaToolbar's naming convention.
    listenTransSetMode() — kanaSetMode('listenTranscribeText', mode, 'listenTranscribeTextKana',  { romaji:'listenTranscribeTex
    setVtDictateMode() — kanaSetMode('vtDictateInput',       mode, 'vtDictateInputKana',       { romaji:'vtDictateInputRomaji
    setGramSentQMode() — kanaSetMode('gramSentQuestion',     mode, 'gramSentQuestionKana',     { romaji:'gramSentQuestionRoma
    setWritingMode() — kanaSetMode('writingInput',         mode, 'writingKana',              { romaji:'writingRomajiBtn',  
    transDoKanjiConvert() — const i=document.getElementById('transInput'); if(i) kanaToKanji(i,document.getElementById('transInp
  Top-level vars: ROMAJI_MAP, _kanaToKanjiLocal
  window[] exports: romajiToHiragana, romajiToKatakana, hiraganaToKatakana, kanaOn, kanaOff, kanaToggle, kanaAddToggle, kanaSetMode, kanaToolbar, kanjiPickerShow, kajiPickerClose, setGlobalQTMode, setChatInputMode, listenTransSetMode, setVtDictateMode, setGramSentQMode, setWritingMode, transDoKanjiConvert
  App registry: engine, romajiToHiragana, romajiToKatakana, hiraganaToKatakana, kanaInputHandler, kanaOn, kanaOff, kanaToggle, kanaAddToggle, kanaSetMode, kanaToolbar, kanjiPickerShow, kajiPickerClose, shims, setGlobalQTMode, setChatInputMode, listenTransSetMode, setVtDictateMode, setGramSentQMode, setWritingMode, transDoKanjiConvert, vgSetupInput
  ⚠ Redundant window[] exports (also in App registry): romajiToHiragana, romajiToKatakana, hiraganaToKatakana, kanaOn, kanaOff, kanaToggle, kanaAddToggle, kanaSetMode, kanaToolbar, kanjiPickerShow, kajiPickerClose, setGlobalQTMode, setChatInputMode, listenTransSetMode, setVtDictateMode, setGramSentQMode, setWritingMode, transDoKanjiConvert
  IIFEs: 4

### src/features-times.js (505 lines)
  Functions:
    minuteReading() — const map = {
    minuteKanji() — if (m === 0) return '';
    openTimesDrill() — document.getElementById('timesDrillOverlay').style.display = 'flex';
    closeTimesDrill() — document.getElementById('timesDrillOverlay').style.display = 'none';
    toggleBlitzMode() — TimesState.blitz = !TimesState.blitz;
    startBlitz() — stopBlitzTimer();
    stopBlitzTimer() — if (TimesState.blitzTimer) { clearInterval(TimesState.blitzTimer); TimesState.blitzTimer = null; }
    blitzTimeUp() — stopBlitzTimer();
    setTimesMode() — TimesState.mode = mode;
    generateTimeQuestion() — numWrong = numWrong || 3;
    generateDateQuestion() — numWrong = numWrong || 3;
    generateQuestion() — if (TimesState.mode === 'time') return generateTimeQuestion(numWrong);
    nextTimesQuestion() — if (DrillFlow._advancing) return;
    playTimesAudio() — if (!TimesState.question) return;
    answerTd() — if (TimesState.blitz && TimesState.blitzLeft <= 0) return;
    startTdTimer() — if (TimesState.blitz) return;
    clearTdTimer() — if (TimesState.timerInterval) { clearInterval(TimesState.timerInterval); TimesState.timerInterval = 
    timeOut() — TimesState.answered = true;
    updateTdScore() — const el = (id) => document.getElementById(id);
    openTimesSettings() — const s = document.getElementById('tdSettings');
    loadTdVoice() — const voices = speechSynthesis.getVoices();
    setTdVoice() — const voices = speechSynthesis.getVoices();
    shuffle() — const a = [...arr];
  Top-level vars: HOUR_READ, HOUR_KANJI, MONTH_READ, MONTH_KANJI, DAY_READ, TimesState
  window[] exports: openTimesDrill, closeTimesDrill, toggleBlitzMode, setTimesMode, nextTimesQuestion, playTimesAudio
  App registry: TimesState, openTimesDrill, closeTimesDrill, toggleBlitzMode, startBlitz, stopBlitzTimer, blitzTimeUp, setTimesMode, generateTimeQuestion, generateDateQuestion, generateQuestion, nextTimesQuestion, playTimesAudio, minuteReading, minuteKanji
  ⚠ Redundant window[] exports (also in App registry): openTimesDrill, closeTimesDrill, toggleBlitzMode, setTimesMode, nextTimesQuestion, playTimesAudio
  IIFEs: 2

### src/briefing-prompt.js (34 lines)
  App registry: BRIEFING_SYSTEM_PROMPT, BRIEFING_FIRST_MSG_TEMPLATE

### src/GrammarModel.js (387 lines)
  Functions:
    GrammarModel() — ═══════════════════════════════════════════════════════
  Top-level vars: GrammarModel
  Storage writes:
    L164 db.run('INSERT')
    L207 db.run('DELETE')
  IIFEs: 1

### src/AppEvents.js (59 lines)
  Functions:
    AppEvents() — ═══════════════════════════════════════════════════════
  Top-level vars: AppEvents
  window[] exports: AppEvents
  IIFEs: 1

### src/features-yoshi.js (5079 lines)
  Functions:
    _fy_claudeAPI() — ═══════════════════════════════════════════════════════
    _fy_claudeText() — ── Phase 3: App-first resolvers ─────────────────────────────────
    _fy_getApiKey() — return (App.getApiKey  || window.getApiKey)?.(); }
    yoshiGetSessions() — return JSON.parse(localStorage.getItem(YOSHI_KEY) || '[]');
    yoshiSaveSessions() — localStorage.setItem(YOSHI_KEY, JSON.stringify(arr));
    yoshiRender() — const sessions = yoshiGetSessions();
    yoshiDeleteSession() — if (!confirm('Delete this session?')) return;
    yoshiOpenSession() — yoshiCurrentIdx = i;
    yoshiLiveTitle() — if (yoshiCurrentIdx === null) return;
    yoshiAttachAudio() — const fileEl = document.getElementById('yoshiAudioFile');
    yoshiKanaDebounce() — clearTimeout(YoshiReadState.kanaTimer);
    yoshiGenKana() — const completeEl = document.getElementById('yoshiComplete');
    yoshiAIComplete() — const clozeEl = document.getElementById('yoshiCloze');
    yoshiAIVocab() — const completeEl = document.getElementById('yoshiComplete');
    yoshiShowVocabPopup() — const existing = document.getElementById('yoshiVocabPopup');
    yoshiVocabPopupAddRow() — const body = document.getElementById('yoshiVocabPopupBody');
    yoshiVocabPopupSave() — const body = document.getElementById('yoshiVocabPopupBody');
    yoshiImport() — const title = document.getElementById('yoshiTitle').value.trim();
    yoshiComputeBlanks() — if (cloze.includes('＿') || cloze.includes('_')) {
    yoshiShowSession() — const main = document.getElementById('yoshiMain');
    yoshiSwitchTab() — document.querySelectorAll('.yoshi-subtab').forEach(b => b.classList.remove('active'));
    yoshiShowImportInline() — const s = yoshiGetSessions()[yoshiCurrentIdx] || {};
    lessonNotesGetSessions() — return LessonNotesState.sessions;
    lessonNotesSaveSessions() — LessonNotesState.sessions = sessions;
    lessonNotesLoadSessionsFromStorage()
    lessonNotesOpen() — LessonNotesState.standalone = true;
    lessonNotesRenderMain() — const el = document.getElementById('lessonNotesViewMain');
    lessonNotesRender() — const el1 = document.getElementById('lessonNotesView');
    lessonNotesRenderPanel() — LessonNotesState.view* — see declaration above
    lessonNotesGetFullPanelHTML() — const sessions = lessonNotesGetSessions();
    lessonNotesPanelHandlePaste() — event.preventDefault();
    lessonNotesPanelHandleDrop() — event.preventDefault();
    lessonNotesPanelHandleFile() — if (!files || files.length === 0) return;
    lessonNotesReadDocxForPanel() — if (typeof mammoth === 'undefined') {
    lessonNotesNewFromPanel() — const title = prompt('Lesson title:');
    lessonNotesDeleteFromPanel() — if (LessonNotesState.currentIdx === null) return;
    lnRecordingTabButton() — ── Recording link helpers ────────────────────────────────────────────────────
    lnShowLinkPicker() — const recs = (window._lessonRecordingSessions || []).filter(function(r) { return !!r.audio_path; });
    lnLinkRecording() — const overlay = document.getElementById('lnLinkPickerOverlay');
    lnTranscribeLinked() — const sessions = lessonNotesGetSessions();
    … +119 more
  Top-level vars: YOSHI_KEY, yoshiCurrentIdx, yoshiAudioURL, YoshiReadState, LessonNotesState, LEARNED_WORDS_KEY, BREAKDOWN_CACHE_KEY
  App registry: panel, lessonNotesRenderPanel, lessonNotesPanelHandleDrop, lessonNotesPanelHandleFile, lessonNotesPanelHandlePaste, lessonNotesNewFromPanel, lessonNotesDeleteFromPanel, lnRecordingTabButton, lnShowLinkPicker, lnLinkRecording, lnTranscribeLinked, lnRenderLinkedRecording, lnLoadTranscript, lnSeekToTime, lessonNotesSetView, lessonNotesToggleGrammarHide, lessonNotesToggleShowHidden, lessonNotesEditGrammar, lessonNotesSaveGrammarEdit, lessonNotesDeleteGrammar, lessonNotesOpenGrammarDetail, lessonNotesCloseGrammarDetail, lessonNotesGrammarGenerateDrill, lessonNotesGrammarCheckDrill, lessonNotesGrammarNextDrill, lessonNotesGrammarPrevDrill, lessonNotesGrammarAsk, lessonNotesOpen, lessonNotesNew, lessonNotesDelete, lessonNotesLoadSession, lessonNotesExtract, lessonNotesExtractGrammar, lessonNotesExtractKeyPhrases, lessonNotesOpenStory, lessonNotesSetMode, lessonNotesDrillReveal, lessonNotesDrillNext, lessonNotesDrillPrev, lessonNotesDrillAll, lessonNotesDrillBackground, renderRecordingsBrowser, recBrowserTranscribe, recBrowserDelete
  Claude prompts:
    L4774 prompt: "You are aligning Yoshi\'s WhatsApp messages to a Japanese lesson audio transcript.\n\n"
    L3397 prompt: "🔊 <button class="btn-primary" onclick="jpSpeak('${…}')">Play</button>"
  Storage writes:
    L581 kvAPI.set('lessonNoteSessions')
  IIFEs: 57

### src/features-grammar.js (2117 lines)
  Functions:
    gdGenerate() — const area = document.getElementById('gdDrillArea');
    gdRenderCard() — const area = document.getElementById('gdDrillArea');
    gdSelectAll() — const boxes = document.querySelectorAll('input[name=gdPreset]');
    gdEnterDrillMode() — const layout = document.getElementById('gdLayout');
    gdExitDrillMode() — const layout = document.getElementById('gdLayout');
    gdToggleDirection() — const val = document.getElementById('gdDirectionValue');
    gdToggleHint() — const hint = document.getElementById('gdHint');
    gdToggleVocabHint() — const hint = document.getElementById('gdVocabHint');
    gdCheck() — if (gdChecked) { gdNext(); return; }
    gdNext() — if (DrillFlow._advancing) return;
    gdRenderComplete() — const area = document.getElementById('gdDrillArea');
    gramSentHistoryLoad() — ── History ────────────────────────────────────────────
    gramSentHistorySave() — if (!target) return;
    gramSentPopulateHistory() — const sel = document.getElementById('gramSentHistory');
    gramSentLoadHistory() — if (!val) return;
    gramSentToggleMode() — ── Mode toggle (manual ↔ auto from error patterns) ───
    gramSentPopulateWeakPoints() — const container = document.getElementById('gramSentWeakPoints');
    gramSentSelectWeakPoint() — const inp = document.getElementById('gramSentInput');
    gramSentCreate() — ── Generation ─────────────────────────────────────────
    gramSentGenerate() — const area = document.getElementById('gramSentDrillArea');
    gramSentRenderCard() — ── Card rendering ─────────────────────────────────────
    gramSentToggleHint() — const hint = document.getElementById('gramSentCardHint');
    gramSentCheck() — ── Checking ───────────────────────────────────────────
    gramSentAdvance() — GramSentState.idx++;
    gramSentSkip() — if (!GramSentState.checked) {
    gramSentFeedbackClick() — const hintArea = document.getElementById('gramSentHintArea');
    gramSentRenderComplete() — ── Complete screen ────────────────────────────────────
    gramSentAskQuestion() — ── Ask a question about the current sentence ──────────
    gramSentInit() — ── Init (called when sent tab opens) ─────────────────
    questionRecordGrammarEvidence() — ═══════════════════════════════════════════════════════
    _unclassifiedAppend()
    unclassifiedRender() — const list = document.getElementById('gnote-unclassified-list');
    unclassifiedClear() — if (!confirm('Clear all unclassified questions?')) return;
    teSetMode() — TeFormState.mode = mode;
    teSetGroup() — TeFormState.group = group;
    conjAddFreqVerbs() — const batch = FREQ_VERBS_ORDERED.slice(_conjFreqVerbsAdded, _conjFreqVerbsAdded + 10);
    conjResetFreqVerbs() — window._conjExtraVerbs = [];
    conjUpdateFreqCount() — const el = document.getElementById('conjFreqVerbCount');
    startConjDrillG() — const ruEndingOnly  = document.getElementById('optRuEndingG')?.checked;
    _conjUpdateSpecialFilterUI() — const ruOnly = document.getElementById('optRuEndingG')?.checked;
    … +21 more
  Top-level vars: gdSentences, gdIdx, gdResults, gdOk, gdChecked, gdConfig, GD_PRESETS, GD_SESSIONS_KEY, GRAM_SENT_HISTORY_KEY, GRAM_SENT_MAX_HISTORY, GramSentState, UNCLASSIFIED_KEY, TE_VERBS, GrammarErrors, _conjTrackingPaused, ConjSession, FREQ_VERBS_ORDERED, _conjFreqVerbsAdded, PARTICLE_PAIR_PROMPTS, _pdPair, _pdExercises, _pdResults, _pdMatchMap, _pdLoading, _pdSelected, _pdShuffles
  window[] exports: conjAddFreqVerbs, conjResetFreqVerbs, particleDrillStart, startConjDrillG
  App registry: gramSentCreate, gramSentGenerate, gramSentLoadHistory, gramSentToggleMode, gramSentCheck, gramSentAdvance, gramSentSkip, gramSentFeedbackClick, gramSentAskQuestion, gramSentToggleHint, gramSentSelectWeakPoint, gramSentInit, writingErrorsClear, questionRecordGrammarEvidence, unclassifiedRender, unclassifiedClear, checkConjG, advanceConjG, conjNextRun, showConjLookupG, retreatConjG, pdSelectLeft, pdSelectRight, pdTryMatch
  Claude prompts:
    L66 prompt: "Generate ${…} Japanese sentences for a ${…} grammar drill targeting: "${…}". ${…}${…} Each sentence must clearly use the target grammar. Incl"
    L469 prompt: "Generate ${…} Japanese sentences for a ${…} learner practising: "${…}". ${…} Each sentence must clearly use the target grammar. Provide a natural English transl…"
    L708 prompt: "Current sentence: "${…}" (${…}). Grammar target: "${…}". Student asks: "${…}". Answer concisely for an N4-N5 learner."
    L757 prompt: "You are classifying a Japanese learner question for a grammar tracking system.\n\n"
  Storage writes:
    L1014 Storage.set('GRAMMAR_ERRORS')
    L1056 Storage.set('CONJ_SESSION')
  IIFEs: 18

### src/features-reading.js (1376 lines)
  Functions:
    qrClose() — document.getElementById('qrOverlay').style.display = 'none';
    qrOnInput() — const val = document.getElementById('qrInput').value.trim();
    qrSpeakRaw() — const text = (document.getElementById('qrInput')?.value || '').trim();
    qrShowPaste() — const rw = document.getElementById('qrReaderWrap');
    qrClear() — const inp = document.getElementById('qrInput');
    qrSendToWrite() — QuickReadState.plainOn — see declaration above
    qrTogglePlain() — QuickReadState.plainOn = !QuickReadState.plainOn;
    qrToggleFuri() — QuickReadState.furiOn = !QuickReadState.furiOn;
    qrSegment() — const rawText = document.getElementById('qrInput').value.trim();
    qrRender() — const container = document.getElementById('qrReader');
    qrWordClick() — if (el.classList.contains('qr-active')) { qrClosePopup(); return; }
    qrPopupHtml() — return `
    qrClosePopup() — if (QuickReadState.popupEl) { QuickReadState.popupEl.remove(); QuickReadState.popupEl = null; }
    vgSetTts() — ── GAME SPEECH + TTS ────────────────────────────────────────────────────
    vgSpeak() — const target = VehicleGameState.targets && VehicleGameState.targets[VehicleGameState.current];
    vgAutoSpeak() — if (VehicleGameState.ttsOn) {
    vgToggleMic() — Vocab game uses custom stt handling
    vgStopMic() — VehicleGameState.micOn = false;
    qrLoadHistoryList() — if (_qrHistoryCache !== null) return _qrHistoryCache;
    qrSaveHistoryList() — _qrHistoryCache = list;
    qrLoadHistoryFromStorage()
    qrUpdateHistoryDropdown() — const select = document.getElementById('qrHistorySelect');
    qrSaveToHistory() — if (!text || text.trim().length < 10) return; // Don't save very short texts
    qrLoadHistory() — if (!indexStr && indexStr !== 0) return;
    qrDeleteHistory() — const select = document.getElementById('qrHistorySelect');
    qrGetRecordingsForStorage() — return Promise.all(QuickReadState.segmentRecordings.map(async (rec) => {
    qrToggleSeparateSentences() — QuickReadState.separateSentences = !QuickReadState.separateSentences;
    qrToggleListenMode() — QuickReadState.sentences* — see declaration above
    qrUpdateSentenceDisplay() — const counter = document.getElementById('qrSentenceCounter');
    qrListenPlay() — const btn = document.getElementById('qrListenPlayBtn');
    qrHighlightSentence() — const reader = document.getElementById('qrReader');
    qrClearSentenceHighlight() — document.querySelectorAll('.qr-sentence-active').forEach(el => {
    qrListenPrev() — if (QuickReadState.sentenceIdx > 0) {
    qrListenNext() — if (QuickReadState.sentenceIdx < QuickReadState.sentences.length - 1) {
    qrSetSpeed() — QuickReadState.listenSpeed = speed;
    qrUpdateSegmentUI() — QuickReadState.recording* — see declaration above
    qrSegDragStart() — QuickReadState.dragIdx — see declaration above
    qrSegDragOver() — e.preventDefault();
    qrSegDrop() — e.preventDefault();
    qrSegDragEnd() — e.target.style.opacity = '1';
    … +19 more
  Top-level vars: QuickReadState, QR_HISTORY_KEY, QR_HISTORY_MAX, _qrHistoryCache, QR_SESSION_KEY
  App registry: qrSpeakRaw, qrClear, qrLoadHistory, qrDeleteHistory, qrToggleSeparateSentences, qrToggleListenMode, qrListenPlay, qrListenPrev, qrListenNext, qrSetSpeed, qrRecordToggle, qrPlaySegment, qrDeleteSegment, qrClearSegments, qrPlayAllSegments, qrCombineSegments, qrDownloadCombined, qrPrintPage, qrSaveSession, qrRestoreSession
  IIFEs: 13

### src/features-video.js (3316 lines)
  Functions:
    _fvid_claudeAPI() — ═══════════════════════════════════════════════════════
    _fvid_getApiKey() — ── Phase 3: App-first resolvers ─────────────────────────────────
    _fvid_showPanel() — return (App.showPanel  || window.showPanel)?.(id); }
    _fvid_Storage() — return App.Storage     || window.Storage; }
    vtToggleWaveMode() — VideoState.waveMode = !VideoState.waveMode;
    vtCollapseLoadBar() — document.getElementById('vtDropZone').style.display = 'none';
    vtDropZoneClick() — document.getElementById('vtVideoInput').click();
    vtHandleDrop() — e.preventDefault();
    vtLoadVideo() — const file = input.files[0];
    vtLoadFile() — const video = document.getElementById('vtVideo');
    vtLoadTranscript() — const file = input.files[0];
    vtLoadTransFile() — const reader = new FileReader();
    vtParseSrt() — const cues = [];
    vtRenderTranscript() — const container = document.getElementById('vtTranscript');
    vtSendToShadow() — const cue = VideoState.cues[idx];
    vtCueClick() — const cue = VideoState.cues[i];
    vtOnTimeUpdate() — const video = document.getElementById('vtVideo');
    vtWatchLoad() — VideoState.watchStartTime → VideoState.watchStartTime
    vtWatchSave() — const today = new Date().toDateString();
    vtWatchUpdate() — const el = document.getElementById('vtWatchMinutes');
    vtWatchStart() — VideoState.watchStartTime = Date.now();
    vtWatchStop() — if (VideoState.watchStartTime) {
    vtOnPlay() — document.getElementById('vtPlayBtn').textContent = '⏸ Pause';
    vtOnPause() — document.getElementById('vtPlayBtn').textContent = '▶ Play';
    vtOnLoaded() — VideoState.watchedThreshold = false;
    vtAnimLoop() — vtDrawWaveform();
    vtTogglePlay() — const v = document.getElementById('vtVideo');
    vtSetSpeed() — document.getElementById('vtVideo').playbackRate = parseFloat(document.getElementById('vtSpeed').valu
    vtUpdateDisplay() — const v = document.getElementById('vtVideo');
    vtFmtTime() — if (!isFinite(s)) return '0:00';
    vtSetLoopA() — const v = document.getElementById('vtVideo');
    vtClearLoop() — VideoState.loopA = null; VideoState.loopB = null; VideoState.loopState = 'idle';
    vtSetLoopPause() — VideoState.loopPauseSecs = secs;
    vtDecodeWaveform() — const waveMsg = document.getElementById('vtWaveMsg');
    vtWaveClick() — const canvas = document.getElementById('vtWaveform');
    vtDrawWaveform() — const canvas = document.getElementById('vtWaveform');
    vtFuriLine() — if (!_fvid_getApiKey()) return;
    vtFuriAll() — if (!_fvid_getApiKey()) return;
    toggleComprehension() — const panel = document.getElementById('compPanel');
    compPopulateVoices() — const all = speechSynthesis.getVoices();
    … +87 more
  Top-level vars: VideoState, VT_WATCH_KEY, ComprehensionState, RESOURCES_KEY, DEFAULT_RESOURCES
  window[] exports: vtToggleShadow, vtShPlayTarget, vtShRecord, vtShPlayback, vtShSeekTarget, resourcesAdd
  App registry: VideoState, core, vtTogglePlay, vtToggleWaveMode, vtCollapseLoadBar, vtDropZoneClick, vtHandleDrop, vtLoadVideo, vtLoadFile, vtLoadTranscript, vtLoadTransFile, vtParseSrt, vtRenderTranscript, vtSendToShadow, vtCueClick, vtOnTimeUpdate, vtWatchLoad, vtWatchSave, vtWatchUpdate, vtWatchStart, vtWatchStop, vtOnPlay, vtOnPause, vtOnLoaded, vtAnimLoop, vtSetSpeed, vtUpdateDisplay, vtFmtTime, vtSetLoopA, vtClearLoop, vtSetLoopPause, vtWaveClick, vtDrawWaveform, undefined, Comprehension, ComprehensionState, toggleComprehension, compPopulateVoices, compGetVoices, compRender, compToggleScript, compSpeakLine, Shadowing, vtToggleShadow, vtShPlayTarget, vtShRecord, vtShPlayback, vtShSeekTarget, setVtDictateMode, resources, vtSaveHistory, vtLoadHistory, vtUpdateHistoryDropdown, vtLoadFromHistory, vtTryLoadMatchingTranscript, vtRenderSavedLinks, vtWordSelect, vtTranslateWord, strokeMagnify, strokeGetReading, vtAddSavedLink, vtShowVocabList, vtCloseVocabList, vtRegenerateVocabList, vtRenderVocabList, vtDeleteVocabWord, vtJumpToTime, vtClearTranslation, vtCloseLineTranslate, readSwitchTab, epubHandleDrop, epubSetFontSize, epubToggleFuri, epubRemoveFuri, epubWireWordTap, epubHidePopup, epubSpeak, epubClose, listenModeChanged, panel, resourcesAdd, resourcesDelete, resourcesReset, resourcesEdit, resourcesSaveEdit
  ⚠ Redundant window[] exports (also in App registry): vtToggleShadow, vtShPlayTarget, vtShRecord, vtShPlayback, vtShSeekTarget, resourcesAdd
  Claude prompts:
    L736 prompt: "Create a short Japanese listening comprehension exercise for ${…} learners on the topic: ${…}. Respond ONLY with valid JSON in this exact format: { "speakerA": …"
  IIFEs: 25

### src/features-pictures.js (521 lines)
  Functions:
    vgSelectTime() — VehicleGameState.time = t;
    vgSelectText() — VehicleGameState.textMode = mode;
    vgSelectCount() — VehicleGameState.count = n;
    vgSelectCat() — VehicleGameState.category = cat;
    vgShowSetup() — document.getElementById('vg-setup').style.display = 'flex';
    vgStart() — VG_VEHICLES = VG_CATEGORIES[VehicleGameState.category].items;
    vgRenderGrid() — const grid = document.getElementById('vg-grid');
    vgNextTarget() — clearInterval(VehicleGameState.timerInt);
    vgUpdateTimer() — const bar = document.getElementById('vg-timer-bar');
    vgOnInput() — const inp = document.getElementById('vg-input');
    vgCheckSilent() — if (!VehicleGameState.running) return;
    vgCheck() — if (!VehicleGameState.running) return;
    vgNext() — if (!VehicleGameState.running) return;
    vgTimeout() — clearInterval(VehicleGameState.timerInt);
    vgEnd() — VehicleGameState.running = false;
  Top-level vars: VG_CATEGORIES, VehicleGameState, VG_VEHICLES
  IIFEs: 3

### src/features-progress.js (1596 lines)
  Functions:
    progressExport() — const data = { _version: 1, _exported: new Date().toISOString(), _app: 'jpStudio' };
    progressImport() — const file = event.target.files?.[0];
    progBackupMsg() — const el = document.getElementById('prog-backup-msg');
    agentGrammarRootSignal() — Given recent error patterns, find nodes implicated and check if
    agentGrammarUnlockSignal() — Walk the graph and find nodes where all prerequisites are satisfied
    drillLastCompletedWrite()
    renderFourStrandRecency() — const el = document.getElementById('drillRecencyGraphic');
    _weekBounds() — const now  = new Date();
    _todayBounds() — const now = new Date();
    _filterHistory() — if (!history || !history.length) return [];
    _accuracyFromHistory() — Falls back to all-time history when period is empty.
    masteryViewSet() — _masteryView = view;
    renderConjMastery() — const el = document.getElementById('conjMasteryPanel');
    renderAdjMastery() — const el = document.getElementById('adjMasteryPanel');
    renderCounterMastery() — const el = document.getElementById('counterMasteryPanel');
    renderGrammarCoverage() — ── Grammar Coverage Grid ─────────────────────────────────────────────────────
    grammarNodeClick() — const node = GrammarModel.getNode(nodeId);
    _pieSliceColors() — const r = parseInt(baseHex.slice(1,3),16)/255;
    _renderErrorPie() — const SIZE = 100, CX = 50, CY = 50, R = 46;
    _renderSubPie() — if (!subSorted.length) return;
    renderErrorList() — Changes here apply to both.
    errorShowPopup() — const examples = (window._errorExamples || {})[key] || [];
    writtenErrorShowPopup() — window._errorExamples = window._writtenErrorExamples || {};
    progressRenderErrors() — const chartEl = document.getElementById('progress-error-chart');
    _renderErrorPieRight() — const SIZE = 120, CX = 60, CY = 60, R = 54;
    particleBreakdownToggle() — const chart  = document.getElementById('particleBreakdownChart');
    particleBreakdownRender() — const chart = document.getElementById('particleBreakdownChart');
    gramSentPracticeError() — showPanel('grammar2');
    weightsRender() — const el = document.getElementById('stPaneWeights');
    weightsSave() — const GM = App.GrammarModel || window.GrammarModel;
    weightsReset() — if (!confirm('Reset all grammar weights to defaults?')) return;
  Top-level vars: PROGRESS_KEYS, ERROR_TYPE_COLORS, ERROR_TYPE_LABELS, N5_GRAPH, DRILL_LAST_COMPLETED_KEY, _masteryView, _origStSwitchTab, GRAMMAR_WEIGHT_LABELS, VOCAB_WEIGHT_INFO
  window[] exports: renderFourStrandRecency, masteryViewSet
  App registry: renderFourStrandRecency, renderConjMastery, renderAdjMastery, renderCounterMastery, renderGrammarCoverage, grammarNodeClick, drillLastCompletedWrite, particleBreakdownToggle, particleBreakdownRender, progressRenderErrors, progressRenderCost, apiUsageReset, apiUsageTrack, gramSentPracticeError, progressExport, progressImport, weightsRender, weightsSave, weightsReset, _renderErrorPieRight, writtenErrorShowPopup, renderErrorList, errorShowPopup
  ⚠ Redundant window[] exports (also in App registry): renderFourStrandRecency
  IIFEs: 16

### src/features-stroke.js (507 lines)
  Functions:
    strokeOnInput() — strokeBuildTiles(inp.value);
    strokeTranslateWord() — const el = document.getElementById('strokeWordMeaning');
    strokeCollapseInput() — const iw = document.getElementById('strokeInputWrap');
    strokeShowInput() — const iw = document.getElementById('strokeInputWrap');
    strokePanelInit() — const inp = document.getElementById('strokeSearchInput');
    handleStrokeKey() — if (e.key === 'Enter') {
    strokeSetSpeed() — StrokeState.speed = parseFloat(v); }
    strokeShowChars() — function strokeAnimate() { strokeBuildTiles(document.getElementById('strokeSearchInput').value); }
    strokeAnimate() — strokeBuildTiles(document.getElementById('strokeSearchInput').value); }
    strokeBuildTiles() — val = (val || '').trim();
    strokeSelectChar() — StrokeState.active = idx;
    strokeLoadKanG() — size = size || 220;
    strokeFetchKoohii() — if (_rtkMnemonicCache[ch]) {
    strokeLoadSteps() — const hex = ch.codePointAt(0).toString(16).padStart(5, '0');
  Top-level vars: StrokeState
  window[] exports: strokeAnimate, strokeReplay, strokeShowChars, strokeSetSpeed, strokeToggleNums
  Storage writes:
    L57 Storage.set('TRANSLATE_CACHE')
    L453 Storage.set('RTK_MNEMONICS')
  IIFEs: 6

### src/features-tools.js (1369 lines)
  Functions:
    lessonNotesUpdateDropdown() — ── Render/utility functions (pure DOM or string parsing — not pipeline logic) ─
    yoshiRenderRecordingTab() — const sessions = window._lessonRecordingSessions || [];
    yoshiParseWhatsapp() — const lines = raw.split('\n');
    lnGetSessions() — const fn = App.lessonNotesGetSessions || window.lessonNotesGetSessions;
    lnSaveSessions() — const fn = App.lessonNotesSaveSessions || window.lessonNotesSaveSessions;
    lnCurrentSession() — const sessions = lnGetSessions();
    lnRenderTab() — if (!cur) return '';
    lnSwitchTab() — document.querySelectorAll('[id^="lnTab-"]').forEach(function(b) {
    lnRenderVocab() — var vocab = cur.vocab || [];
    lnSetDrillMode() — window._lnDrillMode = mode; window._lnDrillRevealed = false; lnRefreshTab('vocab'); }
    lnDrillReveal() — window._lnDrillRevealed = true; lnRefreshTab('vocab'); }
    lnDrillNext() — window._lnDrillIdx = (window._lnDrillIdx||0) + 1; window._lnDrillRevealed = false; lnRefreshTab('voc
    lnDrillPrev() — window._lnDrillIdx = Math.max(0, (window._lnDrillIdx||0) - 1); window._lnDrillRevealed = false; lnRe
    lnDrillJump() — window._lnDrillIdx = i; window._lnDrillRevealed = false; lnRefreshTab('vocab'); }
    lnRefreshTab() — var btn = document.getElementById('lnTab-' + tab);
    lnRenderStories() — var stories = cur.stories || [];
    lnOpenStory() — var cur = lnCurrentSession();
    lnRenderKeyPhrases() — var kp = cur.keyPhrases || [];
    lnRenderCorrections() — var corrections = cur.corrections || [];
    lnRenderGrammar() — var grammar = cur.grammar || [];
    lnToggleGrammarHide() — if (!window._lnGrammarHidden) window._lnGrammarHidden = new Set();
    lnToggleShowHidden() — window._lnShowHidden = !window._lnShowHidden;
    lnRenderTopics() — var topics = cur.topics || [];
    lnRenderFullDoc() — var raw = cur.rawText || '';
    lnRenderRecording() — var recordings = window._lessonRecordingSessions || [];
    lnFilterTranscript() — document.querySelectorAll('.lesson-transcript-row').forEach(function(r) {
    lnToggleRecPlayer() — ── Recording list handlers ──────────────────────────────────────────────────
    lnDeleteRecording() — const recs = window._lessonRecordingSessions || [];
    lnNewSession() — _lnCurrentIdx = null;
    lnLoadSession() — _lnCurrentIdx = idx >= 0 ? idx : null;
    lnDeleteSession() — if (!confirm('Delete this lesson session?')) return;
    lnCreateFromPaste() — var raw = (document.getElementById('lnPasteArea') || {}).value || '';
    lnHandleFile() — if (!files || !files[0]) return;
    lnHandleDrop() — event.preventDefault();
    yoshiSaveWhatsappInline() — const el = document.getElementById('waInput_' + sessionId);
    yoshiRetranscribe()
    yoshiOpenOverlay() — if (window.electronAPI?.openLessonOverlay) {
    yoshiOpenTeams() — const url = 'https://teams.live.com/meet/9387084365462?p=cBmSFutAH5IRELtQxD';
    yoshiTestChannels() — Result is persisted to failure_events for post-session forensics.
    yoshiRunPreflight() — ── Full preflight (optional yoshiRunPreflight() call) ────────────────────────
    … +19 more
  Top-level vars: IMPORTED_DOC_SESSIONS_PANEL_KEY, _lnCurrentIdx, _lnExtracting, _origYoshiInitUI, MatchingPairsState, EpubState, TeFormState, Grammar2State, VoiceState
  window[] exports: yoshiInitUI, addGramEntry, advanceConj, cancelGramEntryEdit, chatQuick, checkConj, checkDictation, checkWritingSentence, clearDictation, clearListenPlaylist, clearTranslate, clearVocabBookmarks, setChatInputMode, clearWritingFeedback, closeTimesDrill, compAnswer, compGenerate, compPlay, compPlayLine, compReplay, compStop, conjSpeak, copyDictation, copySavedText, ctrToggle, cycleLoop, deleteGramEntry, deleteSavedText, deleteVocabBookmark, doTranslate, dropVocabBookmark, editGramEntry, exportAnki, feToggle, flipVocab, gdCheck, gdExitDrillMode, gdGenerate, gdNext, gdSelectAll, gdToggleDirection, gdToggleHint, gdToggleVocabHint, gramSearch, jishoLookup, jpSpeak, kanaToKanji, kanaToggle, listenQueueNext, listenQueuePrev, listenSkip, loadListenTrack, loadTransHistory, markVocab, newDictSentence, nextTimesQuestion, nextVocab, openTimesDrill, openTimesSettings, playDictSentence, playSelected, playTimesAudio, prevVocab, printVocabList, progressExport, qrPrintPage, qrSegment, qrSendToWrite, qrShowPaste, qrToggleFuri, qrTogglePlain, rateListen, resetVocabDeck, revealKanji, rtkSkip, saveApiKey, saveGramEntryEdit, saveWritingText, scoreRtk, seekToBookmark, sendChat, setKanaMode, setRtkDirection, setRtkMode, setTimesMode, showBdTab, showGramTab, showGramTabAndHighlight, showPanel, skipKana, startBlitz, startChatSpeech, startConjDrill, refreshMicrophones, setMicrophone, startNewSession, startTransSpeech, startWritingSpeech, sttStart, submitWritingSentence, testMicrophone, toggleBlitzMode, toggleComprehension, toggleConjListenMode, toggleDetail, toggleDictationMode, toggleGrammar, toggleListenPlay, toggleShadowing, toggleSrsTracking, toggleTransKana, toggleVcReading, toggleVocabList, transAddToVocab, transDoKanjiConvert, transSpeakOutput, listenTransToggleKana, listenTransSetMode, listenTransClear, vcFetch, vcFetchAdd, vcFetchSelectAll, vcToggleHint, vgEnd, vgNext, vgSelectCat, mgSelectCat, mgSetPairs, mgSetTimer, mgSetReading, mgStart, mgEnd, mgShowSetup, vgSelectCount, vgSelectText, vgSelectTime, vgSetTts, vgShowSetup, vgSpeak, vgStart, vgToggleMic, vtAddMarker, vtBreakdownLine, vtClearLoop, vtClearMarkers, vtClearTranslation, vtCloseBreakdown, vtCloseVocabList, vtCueClick, vtDeleteVocabWord, vtDropZoneClick, vtFullscreen, vtFuriAll, vtFuriLine, vtJumpMarker, vtJumpToTime, vtLoadTranscript, vtLoadVideo, vtOnPause, vtOnPlay, vtOpenSaved, vtRegenerateVocabList, vtSetLoopA, vtSetSpeed, vtShowVocabList, vtTogglePlay, vtToggleWaveMode, vtTranslateLine, vtWatchUpdate, vtWaveClick, vtWordSelect, wbCopyInput, wordsSwitchSub, yoshiAIComplete, yoshiAIVocab, yoshiAddVocab, yoshiAttachAudio, yoshiCheckCloze, yoshiConfirmCloze, yoshiImport, yoshiMiniCycleLoop, yoshiMiniCycleLoopEl, yoshiMiniSkip, yoshiMiniSkipEl, yoshiMiniToggle, yoshiMiniToggleEl, yoshiOpenSession, yoshiParseDocx, yoshiReadTTSAll, yoshiReadTTSStop, yoshiReadToggleAll, yoshiResetCloze, yoshiRevealAll, yoshiSwitchTab, yoshiToggleFurigana, yoshiToggleParaEn, yoshiVocabPopupAddRow, yoshiVocabPopupSave, yoshiWordClick, yoshiWordPopupClose
  App registry: lessonNotesUpdateDropdown, yoshiRenderRecordingTab, yoshiParseWhatsapp, lnSwitchTab, lnSetDrillMode, lnDrillReveal, lnDrillNext, lnDrillPrev, lnDrillJump, lnRefreshTab, lnOpenStory, lnToggleGrammarHide, lnToggleShowHidden, lnFilterTranscript, lnToggleRecPlayer, lnDeleteRecording, lnNewSession, lnLoadSession, lnDeleteSession, lnCreateFromPaste, lnHandleFile, lnHandleDrop, yoshiSaveWhatsappInline, yoshiRetranscribe, yoshiInitUI, yoshiOpenOverlay, yoshiOpenTeams, yoshiTestChannels, yoshiRunPreflight, mgSelectCat, mgSetPairs, mgSetTimer, mgSetReading, mgStart, mgEnd, mgShowSetup, mgClickKanji, mgClickEmoji
  ⚠ Redundant window[] exports (also in App registry): yoshiInitUI, mgSelectCat, mgSetPairs, mgSetTimer, mgSetReading, mgStart, mgEnd, mgShowSetup
  IIFEs: 21

### src/features-voice.js (2868 lines)
  Functions:
    _fv_claudeAPI() — fallback. Called as functions so they always pick up the latest
    _fv_claudeText() — registered value (safe even if load order shifts).
    _fv_getApiKey() — return (App.getApiKey      || window.getApiKey)?.(); }
    _fv_showPanel() — return (App.showPanel      || window.showPanel)?.(id); }
    _fv_Storage() — return App.Storage         || window.Storage; }
    _fv_StudentModel() — return App.StudentModel    || window.StudentModel; }
    _fv_GrammarModel() — return App.GrammarModel    || window.GrammarModel; }
    _fv_escHtml() — return (App.escHtml        || window.escHtml)?.(s) ?? s; }
    agentCollectSignals() — const signals = [];
    agentGetLaunchPanel() — const signals = agentCollectSignals();
    agentRefresh() — const btn = document.getElementById('agentRefreshBtn');
    agentRenderCards() — const container = document.getElementById('agentCards');
    agentContextLoad()
    agentContextSave() — try { _fv_Storage().set(AGENT_CONTEXT_KEY, JSON.stringify(profile)); } catch(e) {}
    agentContextToPrompt() — if (!profile || typeof profile !== 'object') return '';
    agentRenderThread() — const thread = document.getElementById('agentClaudeThread');
    briefingTodayKey() — return new Date().toISOString().slice(0, 10); // YYYY-MM-DD
    briefingLoadCache()
    briefingSaveCache() — try { _fv_Storage().setJSON(BRIEFING_CACHE_KEY, { date: briefingTodayKey(), text }); } catch(e) {}
    agentClaudeBriefing() — const btn     = document.getElementById('agentClaudeBtn');
    agentClaudeFollowUp() — const input  = document.getElementById('agentFollowUpInput');
    _agentStream() — const apiKey = _fv_getApiKey();
    agentContextUpdateBtn() — const btn = document.getElementById('agentContextBtn');
    agentToggleContext() — const panel = document.getElementById('agentContextPanel');
    agentContextPopulateFields() — const p = agentContextLoad();
    agentContextSaveFromFields() — const fields = ['nativeLang','otherLangs','currentLevel','targetLevel','aims','dailyTime','lessons',
    agentUpdatePresence()
    openGramNote() — _fv_showPanel('gramnotes');
    pauseDataGet() — try { return JSON.parse(localStorage.getItem(PAUSE_DATA_KEY) || '[]'); } catch { return []; }
    pauseDataAdd() — if (!pauses || !pauses.length) return;
    pauseDataClear() — localStorage.removeItem(PAUSE_DATA_KEY);
    pauseAnalyse() — Extract intra-sentence pauses from Whisper verbose_json segments
    spokenErrorsGet()
    spokenErrorsSave() — _fv_Storage().setJSON(STORAGE_KEYS.SPOKEN_ERRORS_DATA, errors);
    spokenErrorsAdd() — if (!correction) return;
    _spokenErrorRecordGrammarEvidence()
    spokenErrorsClear() — if (!confirm('Clear all spoken error data? This cannot be undone.')) return;
    spokenErrorShowPopup() — window._errorExamples = window._spokenErrorExamples || {};
    renderSpokenErrorsProgress() — const chartEl = document.getElementById('progress-spoken-chart');
    voiceGetProfile()
    … +40 more
  Top-level vars: AGENT_CACHE_KEY, AGENT_CACHE_MINS, AGENT_CONTEXT_KEY, _agentConversation, BRIEFING_CACHE_KEY, SPOKEN_ERRORS_KEY, PAUSE_DATA_KEY, SPOKEN_ERROR_TO_NODE, VOICE_PROFILE_KEY, _origShowPanelVoice
  App registry: briefing, agentRefresh, agentRenderCards, agentClaudeBriefing, agentClaudeFollowUp, agentContextSave, agentToggleContext, agentContextSaveFromFields, agentUpdatePresence, undefined, openGramNote, errors, spokenErrorsClear, renderSpokenErrorsProgress, spokenErrorShowPopup, profile, voiceOpenProfile, voiceSaveProfileFromModal, voiceStartProfileInterview, conversation, voiceDoSave, voicePlayLastRecording, voicePlaySentenceBySentence, voiceSentenceTap, voiceStopSentences, voiceToggleRecord, voiceSendText, voiceReviewErrors, voiceClearChat, voiceNewChat, voiceCheckKeys, voiceSaveConversation, voiceLoadConversation, voiceDeleteConversation, Keys, saveOpenAIKey, getOpenAIKey, js
  Claude prompts:
    L457 systemPrompt: "You are a Japanese language tutor. Answer follow-up questions about the student's learning data concisely. Reference specific data where relevant. No bullet poi…"
    L1169 systemPrompt: "You are conducting a friendly get-to-know-you interview in Japanese with a language learner. Level: ${…} Your task: Ask ONE question to learn about the user. Pi…"
    L1233 systemPrompt: "The user answered an interview question in Japanese. Extract any personal information they shared. Their answer: "${…}" Recent conversation context: ${VoiceStat…"
    L2089 prompt: "Review my Japanese from this conversation. Here are my messages: ${…}. ${…}`).join('\n')} Analyze my errors and give actionable feedback. IMPORTANT: - Some erro…"
  IIFEs: 9

### src/features-voice-drill.js (610 lines)
  Functions:
    voiceSubSwitch() — if (tab !== 'drill' && VoiceDrill.recording) drillStopRecord();
    drillInit() — drillApplyFilter();
    drillApplyFilter() — const cluster = document.getElementById('drillClusterSel').value;
    drillShowCard() — const deck = VoiceDrill.deck;
    drillSpeak() — const s = VoiceDrill.deck[VoiceDrill.pos];
    drillNext() — if (VoiceDrill.pos < VoiceDrill.deck.length - 1) VoiceDrill.pos++;
    drillPrev() — if (VoiceDrill.pos > 0) VoiceDrill.pos--;
    drillMark() — const s = VoiceDrill.deck[VoiceDrill.pos];
    drillToggleRecord() — if (VoiceDrill.recording) {
    drillStartRecord() — const _VoiceState = App.VoiceState || window.VoiceState;
    drillStopRecord() — if (VoiceDrill._mediaRec && VoiceDrill.recording) {
    drillTranscribe() — stream.getTracks().forEach(t => t.stop());
    toHira() — Katakana → hiragana, strip punctuation and slot markers.
    drillScore() — const s = VoiceDrill.deck[VoiceDrill.pos];
    customInit() — const stored = localStorage.getItem(CUSTOM_STORAGE_KEY);
    customBuildDeck() — const el = document.getElementById('customTextarea');
    customShowCard() — const deck = VoiceDrill.customDeck;
    customNext() — if (!VoiceDrill.customDeck.length) return;
    customPrev() — if (!VoiceDrill.customDeck.length) return;
    customToggleRecord() — if (VoiceDrill.customRec) customStopRecord();
    customStartRecord() — const _VoiceState = App.VoiceState || window.VoiceState;
    customStopRecord() — if (VoiceDrill._customRec && VoiceDrill.customRec) {
    customTranscribe() — stream.getTracks().forEach(t => t.stop());
    customScore() — const deck = VoiceDrill.customDeck;
  Top-level vars: DRILL_SOCIAL, VoiceDrill, CUSTOM_STORAGE_KEY
  window[] exports: voiceSubSwitch, drillApplyFilter, drillSpeak, drillNext, drillPrev, drillMark, drillToggleRecord, customBuildDeck, customNext, customPrev, customToggleRecord
  App registry: VoiceDrill, voiceSubSwitch, drillInit, drillApplyFilter, drillShowCard, drillSpeak, drillNext, drillPrev, drillMark, drillToggleRecord, drillScore, customInit, customBuildDeck, customShowCard, customNext, customPrev, customToggleRecord, customStopRecord, customScore
  ⚠ Redundant window[] exports (also in App registry): voiceSubSwitch, drillApplyFilter, drillSpeak, drillNext, drillPrev, drillMark, drillToggleRecord, customBuildDeck, customNext, customPrev, customToggleRecord

### src/features-corpus-seed.js (NOT FOUND)

### src/features-custom-drill.js (579 lines)
  Functions:
    cdAllKeys() — saveDatabase() never fires, nothing survives a restart.
    cdLoadDoc() — if (!window.kvAPI) return null;
    cdSave() — if (!window.kvAPI) {
    cdDelete() — if (!window.kvAPI) return;
    cdShowView() — ['cd-view-list','cd-view-editor','cd-view-drill'].forEach(id => {
    cdInitList() — const keys = await cdAllKeys();
    cdRenderList() — const el = document.getElementById('cd-doc-list');
    cdOpenDoc() — const doc = CDState.docs.find(d => d.id === id) || cdLoad(id);
    cdOpenAndDrill() — const doc = CDState.docs.find(d => d.id === id) || cdLoad(id);
    cdNewDoc() — CDState.current = {
    cdRenderEditor() — const doc = CDState.current;
    cdRenderTable() — const doc = CDState.current;
    _cdEsc() — return (s || '').replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;');
    cdJpInput() — if (CDState.current?.pairs[i]) CDState.current.pairs[i].jp = val;
    cdEnInput() — if (CDState.current?.pairs[i]) CDState.current.pairs[i].en = val;
    cdJpBlur() — if (!CDState.current) return;
    _cdAutoSave() — if (!CDState.current) return;
    _cdUpdateCount() — const doc = CDState.current;
    cdAddRow() — if (!CDState.current) return;
    cdSaveDoc() — if (!CDState.current) return;
    cdDeleteDoc() — if (!CDState.current) return;
    cdBackToList() — await cdInitList();
    cdStartDrill() — const doc = CDState.current;
    cdShowCard() — const pairs = CDState.drillPairs;
    cdNextCard() — if (CDState._myAudio)  { CDState._myAudio.pause();  CDState._myAudio  = null; }
    cdEndDrill() — if (CDState._myAudio)  { CDState._myAudio.pause(); }
    cdPlayTTS() — const pair = CDState.drillPairs[CDState.drillPos];
    cdToggleRecord() — if (CDState.recording) cdStopRecord();
    cdStartRecord() — const _VoiceState = App.VoiceState || window.VoiceState;
    cdStopRecord() — if (!CDState._mediaRec || !CDState.recording) return;
    cdOnRecordStop() — stream.getTracks().forEach(t => t.stop());
    cdScoreRecording() — const pair = CDState.drillPairs[CDState.drillPos];
    cdPlayMyRecording() — if (!CDState._myBlob) return;
    cdTabInit() — await cdInitList();
  Top-level vars: CDState, CD_KEY_PREFIX
  window[] exports: cdNewDoc, _cdAutoSave, cdOpenDoc, cdOpenAndDrill, cdAddRow, cdSaveDoc, cdDeleteDoc, cdBackToList, cdJpBlur, cdJpInput, cdEnInput, cdStartDrill, cdNextCard, cdEndDrill, cdPlayTTS, cdPlayMyRecording, cdToggleRecord
  App registry: CDState, cdTabInit, cdNewDoc, cdOpenDoc, cdOpenAndDrill, cdRenderList, cdRenderEditor, cdRenderTable, cdAddRow, cdSaveDoc, cdDeleteDoc, cdBackToList, cdJpBlur, cdJpInput, cdEnInput, _cdAutoSave, _cdUpdateCount, cdStartDrill, cdShowCard, cdNextCard, cdEndDrill, cdPlayTTS, cdPlayMyRecording, cdToggleRecord
  ⚠ Redundant window[] exports (also in App registry): cdNewDoc, _cdAutoSave, cdOpenDoc, cdOpenAndDrill, cdAddRow, cdSaveDoc, cdDeleteDoc, cdBackToList, cdJpBlur, cdJpInput, cdEnInput, cdStartDrill, cdNextCard, cdEndDrill, cdPlayTTS, cdPlayMyRecording, cdToggleRecord
  IIFEs: 5

### src/StudentModel.js (533 lines)
  Functions:
    StudentModel() — ═══════════════════════════════════════════════════════════════════════════════
  Top-level vars: StudentModel
  IIFEs: 2

### src/IssueTracker.js (471 lines)
  Functions:
    IssueTracker() — ═══════════════════════════════════════════════════════
    issueTrackerRender() — const el = document.getElementById('issueTrackerPanel');
    _itRenderGroupBar() — const bar = document.getElementById('itGroupBar');
    issueTrackerRenderFull() — const container = document.getElementById('issueTrackerFull');
    itAdd() — ── Action handlers ────────────────────────────────────
    itToggleExpand() — const el = document.getElementById(`it-expand-${id}`);
    itToggleSelect() — if (checked) _itSelected.add(id);
    itClearSelection() — _itSelected.clear();
    itCopyGroupPrompt() — if (_itSelected.size < 2) return;
    itUpdateField() — IssueTracker.update(id, { [field]: value });
    itCopyPrompt() — const issues = IssueTracker.getAll();
    itEditResolution() — const issues  = IssueTracker.getAll();
    itDelete() — if (!confirm('Delete this issue?')) return;
    _itEsc() — return (str || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&qu
  Top-level vars: IssueTracker, _itSelected
  App registry: IssueTracker, issueTrackerRender, issueTrackerRenderFull, itAdd, itToggleExpand, itToggleSelect, itClearSelection, itCopyGroupPrompt, itUpdateField, itCopyPrompt, itEditResolution, itDelete
  IIFEs: 10

### src/services/LessonSession.js (113 lines)
  IIFEs: 1

### src/services/AudioService.js (212 lines)
  Functions:
    AudioService() — ═══════════════════════════════════════════════════════
  Top-level vars: AudioService
  window[] exports: AudioService
  IIFEs: 2

### src/services/PreflightService.js (275 lines)
  Functions:
    PreflightService() — ═══════════════════════════════════════════════════════
  Top-level vars: PreflightService
  Storage writes:
    L238 db.run('INSERT')
  IIFEs: 1

### src/services/TranscriptionService.js (142 lines)
  Functions:
    TranscriptionService() — ═══════════════════════════════════════════════════════
  Top-level vars: TranscriptionService
  window[] exports: TranscriptionService
  IIFEs: 1

### src/services/AnalysisService.js (159 lines)
  Functions:
    AnalysisService() — ═══════════════════════════════════════════════════════
  Top-level vars: AnalysisService
  window[] exports: AnalysisService
  Claude prompts:
    L75 prompt: "You are aligning a WhatsApp lesson thread with an audio transcript.\n\n"
    L132 prompt: "Analyse this Japanese lesson. The student is a German-speaking beginner (N5-N4 level).\n\n"
  IIFEs: 1

### src/services/StorageService.js (146 lines)
  Functions:
    StorageService() — ═══════════════════════════════════════════════════════
  Top-level vars: StorageService
  window[] exports: StorageService
  IIFEs: 2

### src/Orchestrator.js (342 lines)
  Functions:
    Orchestrator() — ═══════════════════════════════════════════════════════
    yoshiStartRecording() — ── Backward-compatibility shims ──────────────────────────────────────────────
    yoshiStopRecording() — await Orchestrator.stopLesson();
    yoshiLoadLessonSessions() — return Orchestrator.loadSessions();
    yoshiDeleteLessonSession() — const msg = (audioPath || teacherPath)
    yoshiTranscribeCurrent() — const btn = document.getElementById('overlayTranscribeBtn');
    yoshiSaveWhatsapp() — const el  = document.getElementById('lessonWhatsappPaste');
  Top-level vars: Orchestrator
  App registry: Orchestrator, yoshiXxx, yoshiStartRecording, yoshiStopRecording, yoshiLoadLessonSessions, yoshiDeleteLessonSession, yoshiSaveWhatsapp, yoshiTranscribeCurrent
  IIFEs: 3

### src/ui/YoshiUI.js (217 lines)
  Functions:
    YoshiUI() — ═══════════════════════════════════════════════════════
    yoshiFilterTranscript() — ── Backward-compat exports ────────────────────────────────────────────────────
    yoshiSeekTo() — ── Backward-compat exports ────────────────────────────────────────────────────
    yoshiInitUI() — YoshiUI.wireButtons();
  Top-level vars: YoshiUI
  App registry: YoshiUI, yoshiFilterTranscript, yoshiSeekTo, yoshiInitUI
  IIFEs: 3

### index.html (3111 lines)

### lesson-overlay.html (771 lines)
  Functions: _setRecState, toggleRecording, _setRecStatus, _updateTimer, overlayStartRecording, overlayStopRecording, overlayTranscribe, formatTime, addMarker, renderMarkers, escHtml, doTranslate, renderHistory, _startAudioMonitor, _tickAudio, peak
  IIFEs: 2

### management.html (529 lines)
  Functions: _loadNext

## Summary
  Syntax OK: 42
  Syntax errors: 0
  Total window[] exports: 333 (102 redundant — also in App registry)
  Total App registry entries: 547
  Total functions defined: 1140
